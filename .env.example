# PostgreSQL connection string (Neon / Supabase / local)
DATABASE_URL=postgres://user:password@127.0.0.1:5432/voiceassistweb
# Long random string used to encrypt Payload sessions — generate a new one per environment
PAYLOAD_SECRET=CHANGE_ME
# Email notifications for contact-form submissions (optional — form still works without)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=you@gmail.com
# SMTP_PASS=app-password
# SMTP_FROM=you@gmail.com
# CONTACT_NOTIFY_EMAIL=nahid.mmc41@gmail.com
# Media storage in production (Vercel Blob)
# BLOB_READ_WRITE_TOKEN=
